import { Template } from '../core/types';
/**
 * 筛选 — a standalone search box that filters a chosen target block.
 * Fields are listed from the target block's collection (collectionFrom = target:targetUid).
 */
export declare const filter: Template;
